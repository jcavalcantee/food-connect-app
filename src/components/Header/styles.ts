import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
    header: {
        height: '20%',
        width: '100%',
        alignItems: 'center'
    },

    logo: {
        height: '100%',
        width: '50%',
    }
})